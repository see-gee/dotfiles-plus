#autoload
if [[ _dotfiles_git_staged ]]; then
   $_dotfiles_git_cmd commit -am "Commit from $(whoami) on $(uname -n) @ date %Y-%m-%d_%H%M" "$@"
  return $?
else 
  echo "No files staged for commit."
  return 1
fi
